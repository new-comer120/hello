#include <iostream>
using namespace std;

int main() {
  // First, we will create a variable name myNum, and print it out.
  // It just the "int" type variable, we will explore it in the "Try It Yourself" folder.
  // See you!
  int myNum = 15;
  cout << myNum;
  return 0;
}