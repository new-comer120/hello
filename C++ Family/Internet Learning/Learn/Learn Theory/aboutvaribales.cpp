#include <iostream>
using namespace std;

int main() {
  // and another of variables. So if you assign value to the variable again, that will overwrite the back variable.
  int myNum = 15;
  myNum = 10;
  cout << myNum;
  return 0;
}